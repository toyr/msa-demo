spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:8081
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS

zipkin:
  endpoint: http://localhost:9411/api/v1/spans
  service: hello